/**
 * Text cleaning layer with position tracking.
 *
 * Provides:
 * - Built-in cleaners (HTML stripping, whitespace normalization, etc.)
 * - cleanText() function with TransformationMap building
 * - Custom cleaner composition via pipeline
 */

export * from "./cleaners"
export * from "./cleanText"
