/**
 * Tokenization Layer
 *
 * Exports tokenize() function and Token interface for applying regex patterns
 * to cleaned text and producing citation candidate tokens.
 */

export * from './tokenizer'
