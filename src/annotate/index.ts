/**
 * Citation annotation API for marking up citations in text.
 *
 * Provides dual-mode annotation:
 * - Template mode: Simple before/after wrapping
 * - Callback mode: Custom annotation logic with full citation context
 *
 * @module annotate
 */

export * from "./annotate"
export * from "./types"
