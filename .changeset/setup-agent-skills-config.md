---
"eyecite-ts": patch
---

Add agent skills configuration: `docs/agents/` (issue tracker, triage labels, domain docs) and an `## Agent skills` section in CLAUDE.md. Documentation/tooling only — no runtime changes.
